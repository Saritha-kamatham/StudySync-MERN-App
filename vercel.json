{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "buildCommand": "npm run build"
      }
    },
    {
      "src": "server/package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "/client/dist/index.html"
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server/server.js"
    },
    {
      "source": "/(.*)",
      "destination": "/client/dist/index.html"
    }
  ],
  "build": {
    "env": {
      "VITE_API_URL": "https://study-sync-mern-project.vercel.app"
    }
  },
  "env": {
    "NODE_ENV": "production",
    "VITE_API_URL": "https://study-sync-mern-project.vercel.app"
  },
  "functions": {
    "server/server.js": {
      "maxDuration": 30
    }
  }
}
